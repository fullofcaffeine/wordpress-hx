package wphx.fixtures.compiler.php.wholefile;

/**
	Compile anchor for the whole-file `wp-includes/class-http.php` pilot.
**/
class ClassHttpEntry
{
	static function main():Void
	{
		Type.getClassName(ClassHttpScript);
	}
}
