#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"

if [ -f "$ROOT_DIR/.beads/issues.jsonl" ]; then
  echo "[post-merge] Importing Beads JSONL..."
  bd import --input "$ROOT_DIR/.beads/issues.jsonl" >/dev/null
  bd sync --flush-only >/dev/null
fi

